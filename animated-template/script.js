AOS.init({
    delay: 200,         // values from 0 to 3000, with step 50ms
    duration: 1500,     // values from 0 to 3000, with step 50ms
    once: false,        // wether animation should happen only once - while scrolling
    mirror: false,      // wether elements should animate out while scrolling
  });